МЕТОДЫ МНОЖЕСТВ


Напомню, что такое множество.
Множество - структура данных, которая представляет собой неупорядоченную коллекцию уникальных элементов.


МЕТОДЫ.

- .add()
  С помощью данного метода мы можем добавить элемент во множесто.
  Синтаксис:
  {множество}.add({элемент}),
  то есть у множества нужно обратиться к этому методу по ключевому слову add,
  а в скобки нужно передать элемент, который нужно добавить
  Стоит заметить, что элемент будет добавляться в рандомное место множество,
  так как множество является неупорядоченной коллекцией, то есть его элементы не имеют нумерации.

- .remove(), .discard()
  C помощью этих двух методов мы можем удалить элемент из множества.
  Синтаксис:
  {множество}.remove({элемент}) или {множество}.discard({элемент}),
  то есть у множества нужно обратиться к одному из этих методов по ключевым слову remove или discard,
  а в скобки нужно передать элемент, который нужно удалить
  Обратите внимание на разницу этих методов:
  Если мы вызовем метод remove и передадим туда элемент, которого нет во множестве,
  то в таком случае мы получим ошибку KeyError.
  А если проделаем те же действия, но с методом discard, то мы не получим никакого результата, в том числе и ошибку.

- .union()
  C помощью этого метода мы можем объединять множества в одно множество.
  Синтаксис:
  {первое_множество}.union({другие множества через запятую}),
  то есть у множества нужно обратиться к этому методу по ключевому слову union,
  а в скобки нужно передать множества, которые хотите объединить с тем множеством, у которого был вызван данный метод.

- in
  С помощью данного ключевого слова можно проверить входит ли элемент во множество.
  Синтаксис:
  {элемент} in {множество},
  то есть сначала прописываем элемент, который хотим проверить на вхождение во множество,
  далее следует ключевое слово in,
  а после множество, в котором хотите проверить на наличие элемент.
  Результатом выполнения данной строки будет булево значение: True или False.
  True - если элемент есть во множестве, False - если элемента нет во множестве.
